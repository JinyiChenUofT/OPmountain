# -*- coding: utf-8 -*-
"""prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1yif_zGlxO7aQj15Hsq3VPuR4J1fx9Lvd
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import mpl_toolkits
from IPython import embed
import pickle
import scipy
import xgboost as xgb
from xgboost import XGBClassifier
from sklearn import metrics
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.tree import DecisionTreeRegressor
from sklearn.metrics import make_scorer
from sklearn.metrics import mean_squared_error,median_absolute_error,r2_score,mean_absolute_error
import pickle
ready_processed=1
#load csv format file 

FLAGS=None
data = pd.read_csv('Housing.csv')

#with column has
all_date = ready_processed

X = data[['lotsize','bedrooms','bathrms','stories']]
Y = data['price']

#

X_train, X_test, y_train, y_test = train_test_split(X,Y,test_size=0.2)

#create regression model and fit training data

bst = XGBClassifier(
    silent=False,
    objective='binary:logistic',
    nthread=-1,
    seed=42,
)
bst.fit(X_train, y_train)

pickle.dump(bst, open(".../model.pkl","wb"))
# comment out ploting things out

from google.colab import files
uploaded = files.upload()